apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "poolparty-workbench.fullname.configmap.environment" . }}
  namespace: {{ include "poolparty-workbench.namespace" . }}
  labels:
    {{- include "poolparty-workbench.labels" . | nindent 4 }}
  {{- with .Values.annotations }}
  annotations:
    {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
data:
  PP_JAVA_OPTS: >-
    {{ tpl .Values.configuration.defaultJavaArguments . }}
    {{ tpl .Values.configuration.javaArguments . }}
