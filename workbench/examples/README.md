# Minimal configuration

The current example provides details about the minimal set of configurations that should be provided to the application,
for proper communication with PoolParty and Keycloak.

## Prerequisites

* PoolParty with all dependencies

## Installation

```shell
helm upgrade --install --values values.yaml adf poolparty-semantic-suite/semantic-workbench
```
